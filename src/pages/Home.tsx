import VerticalLayout from "@components/layout/VerticalLayout";

const Home = () => {
  return (
    <VerticalLayout pageTitle="Home">
      <div id="/">Home</div>
    </VerticalLayout>
  );
};

export default Home;
